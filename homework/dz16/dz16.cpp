#include <iostream>
#include "museum.h"
#include "student.h"
using namespace std;

int main(){
    // prob 6
    // Exposures expo[] = {{"archeology", 48}, {"geology", 25}, {"paleontology", 60}, {"zoology", 45}, {"botany", 52}};
    // int expoQuantity = 5;

    // Ticket ticket;

    // cout << "How many exposures do you want to visit? ";
    // cin >> ticket.num_choices;
    // if (ticket.num_choices > ticket.MAX_CHOICE || ticket.num_choices <= 0) cout << "There is no such quantity of the exposures\n";
    // else{
    //     cout << "Enter the exposures names: ";
    //     for (int i = 0; i < ticket.num_choices; i++) {
    //         cin >> ticket.choice[i];
    //     }

    //     ticket.print_ticket(expo, expoQuantity);
    // }


    // prob 7
    // Student student;
    // student.get_student();

    // student.status();
    // student.eat();
    // student.sleep();
    // student.study();
    // student.status();

    // student.study();
    // student.status();

    return 0;
}